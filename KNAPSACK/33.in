#include

using namespace std;

typedef long long ll;
int N,S,dp[4009];
int v[2004],s[2004];

int main()
{
    cin>>S>>N;
    for(int i=0;i>s[i]>>v[i];
    memset(dp, 0 , sizeof dp);
    for(int i=0;i